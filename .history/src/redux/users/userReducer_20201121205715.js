import _getUsers from "../../_DATA";
import {GET_USERS_SUCCESS} from './'

const initialState = {
    users: []
}

const user = (state= initialState, action){
    switch(action.type){
        case 
    }
}